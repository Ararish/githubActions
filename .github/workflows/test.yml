name: test actions

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: deploy
        run: echo "Hello, world!"
             cd /home/ubuntu/projects/githubActoins
             git pull origin main
        
